services:
  - type: web
    name: fitcoach-tts
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: ELEVEN_API_KEY
        sync: false
